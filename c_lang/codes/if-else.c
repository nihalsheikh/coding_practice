#include <stdio.h>

int main()
{
    // if-else statement use
    int num = 10;
    if(num > 0) {
        printf("Number is positive.\n");
    }
    else {
        printf("Number is negative.\n");
    }

    return 0;
}
